<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Menú Principal Appix</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <%- include('mostrar_riesgos.style.ejs') %>
    <%- include('./../barra_principal.ejs')%>
    <nav class="boton" aria-label="Page navigation example">
        <ul class="pagination">
          <li class="page-item">
            <a class="page-link" href="/menu_proyecto" aria-label="Previous">
              <span aria-hidden="true">&laquo;</span>
            </a>
        </ul>
      </nav>
    <div class="row justify-content-center mt-6">
        <div class="col-md-4">
            <div class="text-center">
                <h3>Riesgos existentes del proyecto</h3>
            </div>
        </div>
    </div>
    <form id="form_crear_riesgo" method="POST" action="/proyecto/mostrar_riesgos">
        <div class="container">
            <div class="table-responsive">
                <table class="table align-middle">
                    <thead>
                        <tr>
                            <th scope="col">Descripcion</th>
                            <th scope="col">Impacto</th>
                            <th scope="col">Categoria</th>
                            <th scope="col">Probabilidad</th>
                            <th scope="col">Estrategia</th>
                            <th scope="col">Seleccionar</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for (var i = 0; i< riesgo.length ; i++){ %>
                        <tr>
                            <td><input type="text" class="form-control" name="descripcion" value="<%= riesgo[i].descripcion %>" readonly></td>
                            <td><input type="number" class="form-control" name="impacto" value="<%= riesgo[i].impacto %>" readonly></td>
                            <td><input type="text" class="form-control" name="categoria" value="<%= riesgo[i].categoria %>" readonly></td>
                            <td><input type="number" class="form-control" name="probabilidad" value="<%= riesgo[i].probabilidad %>" readonly></td>
                            <td><input type="text" class="form-control" name="estrategia" value="<%= riesgo[i].estrategia_m %>" readonly></td>
                        </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>
        <div class="container py-4">
            <button type="submit" class="btn btn-primary mb-3">Editar</button>
        </div>
    </form>
</body>
<%- include('mostrar_riesgos.js.ejs')%>
</html>