<style>
    body {
    background-image: url('/proyecto/FONDO_web_N-03.png');
    background-size: cover; /* Cubrir todo el fondo */
    background-position: center bottom 1rem; /* Centrar la imagen en la página */
    background-attachment: fixed;
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    border: none; /* Asegura que no se apliquen bordes globalmente */
    background: none; /* Elimina fondos globales si es necesario */
}
.circle-container {
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
    background: transparent;
}

.container {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    width: 100%;
    max-width: 980px; /* Ajustado para un mejor fit */
    margin: 50px auto 0 auto; /* Ajustado el margen superior para subir el contenido */
    padding: 0px;
    background: transparent; /* Establece el fondo a transparente */
    box-shadow: none; /* Elimina cualquier sombra */
}
.progress {
    width: 150px;
    height: 150px;
    border-radius: 50%;
    background: #444 linear-gradient(to right, transparent 50%, var(--clr) 0); /* Crea un efecto de progreso */
    color: #fff; /* Texto blanco */
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
}

.progress h3 {
    font-size: 2.5em;
    z-index: 1;
    font-weight: 500;
}

.progress h3 span {
    font-size: 0.65em;
    font-weight: 400;
}

.progress::after {
    content: '';
    position: absolute;
    inset: 20px;
    border-radius: 50%;
    background: #222; /* Fondo oscuro para el círculo interior */
}

.d-flex {
    display: flex;
    justify-content: space-between;
    align-items: center;
    width: 100%;
}

.progress-container {
    flex-grow: 1; /* Permite que el contenedor de la gráfica crezca y se acerque a los botones */
    display: flex;
    justify-content: flex-end; /* Alinea la gráfica hacia el final del contenedor (hacia los botones) */
    margin-right: 150px;
}

.progress {
    position: relative;
    width: 270px; /* Aumentado de 150px a 200px */
    height: 270px; /* Aumentado de 150px a 200px */
    border-radius: 50%;
    color: #fff;
    background: #444 linear-gradient(to right, transparent 50%, var(--clr) 0);
}

.progress h3 {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    font-size: 3em; /* Aumentado para ajustarse al nuevo tamaño */
    z-index: 1;
    font-weight: 500;
}

.progress h3 span {
    font-size: 0.75em; /* Ajustado proporcionalmente */
}
.info-container {
    text-align: left;
    margin-right: 20px; /* Ajuste para acercar a la gráfica */
}

.info-container span, .info-container p {
    font-size: 18px; /* Ajusta este valor según necesites */
}

.info-container .risk-points {
    font-size: 20px; /* Hace que los números sean un poco más grandes si lo deseas */
}

.status-text {
    font-size: 24px; /* Ajusta este valor según tus necesidades */
}

.buttons {
    display: flex;
    flex-direction: column;
    gap: 15px; /* Espacio entre los botones */
}

button {
    width: 100%; /* Asegura que los botones ocupen todo el ancho de su contenedor */
    padding: 10px 0;
    font-size: 20px;
}

.header {
    margin-bottom: 20px;
}

.risk-points {
    font-weight: bold; /* Negrita para los puntos de riesgo */
}

</style>